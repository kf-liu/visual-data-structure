export default ({ className }: { className?: string }) => {
    return (
        <p className={className}>
            可视化数据结构
            <br />
            Visual Data Structure
        </p>
    )
}